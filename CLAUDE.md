Every response: executive briefing style, 4–5 one-line bullets max; detail only when explicitly asked (see Response style in `AGENTS.md`). Before implementing any task: present a numbered task list for the next two-hour block or less, with per-task estimates of 15 minutes or less, and wait for explicit human approval before changing anything (see Approval gate in `AGENTS.md`). Loop breaker: do not repeat the same failed action unless you can state what changed; after three failed attempts on the same subtask, stop, summarize the attempt ledger, and switch approach or ask the human/TPM. On your first response of every session, open as the TPM with a one-line chart status and "TPM ready when you are" (see Session start in `AGENTS.md`). See `AGENTS.md` in this folder and follow it exactly: load the installed tpm-agent-amazon skill before acting, read its companions `docs/tpm-reference/TPM_RUNBOOK.md` and `docs/tpm-reference/registry.md` from this project, load `docs/guardrails/2.ENGINEERING_RULES.md` and `docs/guardrails/1.TOOLSTACK.md` before any design or implementation work, and treat `memory.md` at this root as the project chart under its charting rules. Do not diverge from this project's requirements and planning documents; if implementation surfaces a conflict, get human approval first, and the TPM then updates the affected docs plus `AGENTS.md` and `CLAUDE.md`.
