# Decision Log

Canonical slot per TPM_RUNBOOK (no equivalent existed in the numbered chain; existing-slot test run 2026-07-06). Every gate-2 approval, door classification, and override use is recorded here. A decision not in this log did not happen.

| # | Date | Decision | Door | Decided by | What it traded away |
|---|---|---|---|---|---|
| 1 | 2026-07-06 | GATE A (semantic-layer refactor, 7 rulings): (1) net_revenue_usd collapses into total_net_revenue; (2) token fact stored at minute grain; (3) customer_quota_weekly_history renamed fct_quota_weekly, stays weekly; (4) docs/10 metric count reconciled 69→73→52; (5) 17-column seed schema, seed is the only metric-authoring surface; (6) WoW/MoM/YTD reimplemented as derived/cumulative MetricFlow types, parity-gated; (7) Task 6 split 6a/6b. Designs: tmp/TASK1_DESIGN.md, tmp/TASK2_DESIGN.md. | Two-way (parity gate 7a protects reversal) | Human | Grain-suffixed metric names; hand-written metric YAML as authoring surface. |
| 2 | 2026-07-06 | Vendored agent reference docs: `agents-reference/` holds read-only stamped copies of ENGINEERING_RULES.md, TPM_RUNBOOK.md, registry.md; masters stay in `~/Documents/kanja-agents/`; refresh only via `refresh-agent-docs.sh`. Family-wide standard (project template updated). Supersedes 2026-07-05 "no in-repo copies" decision. Motive: sandbox-scoped hosts (Cowork) cannot read sibling folders. | Two-way (delete copies, revert pointers) | Human | Single-location purity; adds a refresh-discipline obligation to prevent doc drift. |
| 3 | 2026-07-06 | Refactor resequenced to comply with ENGINEERING_RULES §1 / runbook MD-first: metric dictionary (docs/10) update + human sign-off moves BEFORE transformation code (was Task 8, now precedes Task 3). | One-way for the program (rule compliance) | TPM (rule-driven), human-informed | Nothing; prevented a compliance violation. |
| 4 | 2026-07-06 | Per-case-study doc chains from day one: numbered WB chain at `docs/`; `docs/` holds both the product chain and program-wide artifacts (DECISION_LOG.md, agents-reference/, README.md). Future case studies get their own `docs/<case-study>/` chain. Includes 8d: stale `doc/` inside the single-dbt-opensource submodule deleted, replaced by a README pointer to the parent chain. | Two-way (moves revert cleanly) | Human | Root-level chain simplicity; path references updated across AGENTS.md, chart, tracker, and chain cross-links. |
| 5 | 2026-07-06 | Drift guard for vendored reference docs: every family skill's SKILL.md now carries a session-start rule — vendor `docs/agents-reference/` on first run (with human permission), rerun `refresh-agent-docs.sh` when masters are reachable (idempotent; refreshed copies surface in git status), proceed on vendored copies when sandboxed. Applied to all 20 skills in kanja-agents. | Two-way (delete the SKILL.md blocks) | Human | Nothing automatic in sandboxed hosts — they stay at-most-one-host-session stale; Cowork's installed skill copies need a one-time update in Settings to pick up the new blocks. |
| 6 | 2026-07-06 | FK/PK metadata home + enforcement scope (data-architect-databricks ruling): fact→dim relationships and surrogate/natural keys are declared once in the dbt model layer (constraint blocks + relationship tests), which is the authoritative, engine-independent source. Database-level enforcement is dev-only via target-gated contract (`contract.enforced: "{{ target.name == 'dev' }}"`) — dev enforces PK/FK/types to catch bad rows at build; prod (trillion-row target) enforces nothing structural on the write path, relying on pipeline idempotency + relationship tests in CI. The semantic-layer generator reads relationships from `manifest.json`, NOT `information_schema`. Grain and the relationships themselves unchanged. | One-way (generator output + semantic layer build on it) | Human (TPM-routed, Data Architect ruling) | `information_schema` as generator introspection source; prod-side referential enforcement. Redefines the constraints step (was "PK/FK visible in information_schema") and the generator step (input becomes manifest, not catalog). |
| 7 | 2026-07-09 | GATE-2 (Step 1, backend operability): approved the durable boot fix — pinned `conversational-bi/requirements.txt` (Python 3.11) + `conversational-bi/README.md` boot recipe, authored by senior-software-engineer-amazon. Verified GREEN on a fresh venv: `pip install -r` clean, `mf 0.13.0` in-venv, `Catalog loaded: 69 metrics`, `/health` = HTTP 200 `catalog_loaded:true`. Root cause of the never-reachable gate: no requirements/venv/recipe existed since C2; Presidio `PIIRedactor()` loads spaCy `en_core_web_lg` at import (hard boot dep, now pinned via wheel); OTLP exporter imports unconditionally. Direct-dep pins only (not a full freeze). | Two-way (delete the 2 files) | Human | Full `pip freeze` cross-machine reproducibility (deferred; offer stands). Unblocks Steps 2 (health scored) and 7 (TR12 eval). |
| 8 | 2026-07-09 | GATE-2 (Step 5, TR12 eval harness): approved ai-architect's defect fix to `conversational-bi/eval/eval_config.yaml`. Root cause: `postprocess` reduced output to a bare `category` string, hiding `metric` from downstream asserts, which also used a non-promptfoo `condition:`/`assert_fail_message:` shape → metric selection was never asserted. Fix: dropped postprocess; category + metric assertions now `type: javascript` under `value:` reading the full `/query` object; metric assert fails on mismatch, no-ops when no expected_metric; domain assert removed (a test-org tag, not a response-contract field). Field names confirmed against real `/query`; assert block validated via `promptfoo validate`; PASS/FAIL walkthrough proven. | Two-way (revert the file) | Human | Coverage of dimensions/time_range (group-by/filters) — DEFERRED (scope-to-increment; current bar is metric selection). Two open items surfaced: (a) route fixture conversion (jsonl→csv/vars) to ai-architect to unblock Step 7; (b) TRD TR12 wording reconciliation (category-routing vs metric-selection bar) routed to solutions-architect. |
| 8b | 2026-07-09 | Harness follow-up (found on first real scoring run): the eval reported 25/25=100% but ZERO assertions ran — `assert:` was at config ROOT; promptfoo only applies global asserts under `defaultTest.assert` for CSV-loaded tests. Moved assert block → now discriminates (verified: 25/25 rows carry 2 assertion components, mismatched rows fail). NEW gate-1 rule for eval harnesses: green only when a REAL run FAILS a known-wrong row (not `validate` / not a manual walkthrough). Then reconciled fixture `expected_metric` from invalid friendly names (revenue/total_tokens/…) to real 69-metric catalog names. | Two-way | Human (approved harness+model track) | The false 100%; measuring against invalid ground truth. |
| 9 | 2026-07-09 | Model swap to beat the ollama baseline: provider set to opencode (OpenCode Zen) via the OpenAI-COMPATIBLE code path — `LLM_PROVIDER=openai`, `OPENAI_BASE_URL=https://opencode.ai/zen/v1`, `OPENAI_MODEL=deepseek-v4-pro`, key in gitignored `.env` (NOT OpenAI the company; the base URL routes all calls to opencode — proven via /models + smoke). JSON mode works with deepseek (no response_format 400). RESULT (harness now truthful): deepseek-v4-pro **overall 64% / category 68% / metric 83%(5/6)** vs ollama **56% / 60% / 83%** — deepseek wins category+overall, ties metric. `_call_openai` hardcodes response_format:json_object (llm_client.py:127) — model must support JSON mode. | Two-way (change `.env`) | Human (chose better model, provider=opencode, key) | Local/offline ollama; a rotated key (the in-file opencode key was exposed in transcript — MUST rotate). |
| 10 | 2026-07-09 | Fixture ground-truth correction (data-architect ruling, grounded in /catalog + docs/10 + TR12): 8 eval rows were mislabeled `answered` with empty metric (self-inconsistent). Corrected `expected_category`: recurring-revenue, revenue-growth-QoQ, input/output token COST, remaining-quota, quota-peak-forecast → `unknown_metric` (domain covered, metric absent); quota-utilization-% and quota-used → `ambiguous` (two governed metrics each). Rows headcount(HR)/support-tickets confirmed CORRECT-AS-IS `unsupported_domain` (a model returning unknown_metric there is a REAL miss). ai-architect applying + re-running deepseek for the clean number. | Two-way (revert CSV) | Human (approved label correction) | 64%-against-shaky-labels; several rows' `answered` label. |
| 12 | 2026-07-11 | GATE-2 (Step 2 / 2a — dbt RI fix committed). Removed the broken PK/FK `post_hook` pattern from 3 dims + 3 facts. **data-architect ruling: ACCEPT** — fact→dim RI now enforced solely by dbt `relationships` tests (9/9 edges) + `not_null` on FK cols + `unique`/`not_null` on dim PKs; zero coverage lost; manifest-based semantic generator unaffected (never read `information_schema`). **Verified:** `dbt build --target prod` green ×2 identical (484 PASS, 0 constraint break), 24 RI tests pass, marts populated (fct_revenue_daily 1,820). **CORRECTION of an earlier TPM claim:** the every-2nd-build break only fires on dev/local targets where the post_hooks run; the in-repo `profiles.yml` defaults to `prod` (post_hooks gated `else []`, never ran), so this was NOT the fresh-clone blocker. The real `--target prod` fresh-clone failure is the `int_revenue_daily` contract (task 2b). **2c/2d (restore 8 retired metric families) DEFERRED** as coverage-expansion beyond the reliability goal — logged, not dropped. | Two-way (restore post_hooks) | Human | Dev-side build-time DB FK/PK enforcement (non-authoritative under #6, never implemented via contract blocks); 8-family catalog coverage this pass. |
| 11 | 2026-07-11 | GATE-2 (Step 1 / M1 close). **1a — accuracy proven:** metric-routing on the widened answerable set (8→23 rows, every `expected_metric` ground-truthed vs the 44 served catalog) = **98.6% aggregate, 95.7% worst run, 0 hallucinations / 126 responses** across 3 seed-pinned runs (temp0/seed42, deepseek-v4-pro). Clears the ≥95% bar on the FLOOR, not just the mean. Commits: `d3d5747` (ranking/argmax guard), `c4551c4` (widened eval). **1b — FR2 verified:** MetricFlow-only, no raw-SQL path for user questions (104 `mf query` invocations this session, zero raw SQL). **1c — FR4a/FR4b clarify-and-continue DEFERRED** (not in this release): the ≥95% bar is met without it; deferred, not dropped (tracked as plan task 1.5). Sole residual miss + one non-answerable over-answer are both the known deepseek temp-0 jitter — fail-safe/in-catalog, never a fabricated answer. | 1c defer is Two-way (build later) but keeps the underlying **one-way door CLOSED** (making `ambiguous` non-terminal changes the 5-category contract) | Human (defer) | The 2-row clarifiable/ambiguous coverage slice; a multi-turn conversational feel this release. Hard every-run determinism guarantee = model/provider question (ai-research), out of scope. |
